# Temporary data (scratch area)
Temp/

# Node
node_modules/

# Environment / secrets
.env
.env.*
*.local

# But keep env templates
!.env.example
!deploy/.env.example

# Logs (process logs are committed via Logs/README.md policy; transient files ignored)
*.log
