name: HuckleberryFinn

role: Architect

model: auto

memory: state.json

workspace: ../../

docs: ../../../docs

repositories: ../../../docs/repositories

skills: ../../../agents/skills

approval_required: true

language: ru

# Researcher и Auditor — режимы работы HuckleberryFinn, а не отдельные агенты
modes:
  research: ../../../agents/configs/Researcher/system.md
  audit: ../../../agents/configs/Auditor/system.md
